<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>자유게시판</title>
    <%- include("./include/include", { title: "자유게시판" }) %>
    <link rel="stylesheet" href="/static/css/board.css" />
  </head>
  <body>
    <div class="header"><%- include("./include/header") %></div>
    <br /><br />
    <h1>자유게시판</h1>
    <div class="board-list">
      <table>
        <thead>
          <tr>
            <th>분류</th>
            <th>작성일</th>
            <th>작성자</th>
            <th>제목</th>
          </tr>
        </thead>
        <tbody>
          <% if (boardList && boardList.length > 0) { %> <%
          boardList.forEach(function (item) { %>
          <tr>
            // 분류 부분은 필요한 항목으로 수정 가능 현재 boardId
            <td><%= item.boardId %></td>
            <td>
              <a href="/board/view/<%= item.boardId %>">
                <%= item.boardTitle %>
              </a>
            </td>
            <td>
              <%= item.boardDate ? item.boardDate.toLocaleString() : "" %>
            </td>
            <td><%= item.userId ? item.userId : "" %></td>
          </tr>
          <% }) %> <% } else { %>

          <h1>자유게시판</h1>
          <tr>
            <td colspan="4">게시글이 없습니다.</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
    <% if (isLogin) { %>
    <div class="write-btn">
      <a href="/board/write" class="btn-red">글 작성</a>
    </div>
    <% } else { %>
    <div class="write-btn">
      <p>
        글을 작성하려면
        <a href="/login" class="btn-red">로그인</a>
        하세요.
      </p>
    </div>
    <% } %>

    <script src="/static/js/board.js"></script>
  </body>
</html>
